# Docker production (.env on VPS — never commit real secrets)
DB_PASSWORD=change-me-use-openssl-rand-hex-16
JWT_SECRET=change-me-use-openssl-rand-hex-32
API_PORT=8081
JWT_EXPIRY_HOURS=72
SHIPPING_FEE_LAK=30000
FREE_SHIPPING_MIN_SUBTOTAL_LAK=500000
